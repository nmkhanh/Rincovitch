<!-- TimelinePage.xaml — Gantt/timeline view of tasks per user (DataContext: TimelineViewModel) -->
<UserControl
  x:Class="NMKApp.Views.TimelinePage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DesignHeight="700"
  d:DesignWidth="1100">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!--#region Toolbar -->
    <Border Grid.Row="0"
            Background="{DynamicResource SecondaryBackgroundBrush}"
            BorderThickness="0 0 0 1" BorderBrush="{DynamicResource PrimaryBorderBrush}"
            Padding="12 6">
      <WrapPanel>
        <TextBlock Text="From" VerticalAlignment="Center" Margin="0 0 6 0" FontSize="12"/>
        <DatePicker Width="120" Height="28"
                    SelectedDate="{Binding MinDay, Mode=TwoWay}"
                    Margin="0 0 8 0"/>
        <TextBlock Text="To" VerticalAlignment="Center" Margin="0 0 6 0" FontSize="12"/>
        <DatePicker Width="120" Height="28"
                    SelectedDate="{Binding MaxDay, Mode=TwoWay}"
                    Margin="0 0 8 0"/>
        <Button Content="Apply"
                Command="{Binding FilterDayCommand}"
                Style="{StaticResource ButtonSecondaryStyle}"
                Margin="0 0 12 0"/>
        <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="4 0"/>
        <!-- Status filter checkboxes from parent window -->
        <ItemsControl ItemsSource="{Binding DataContext.FiltersStatus, RelativeSource={RelativeSource AncestorType=Window}}"
                      VerticalAlignment="Center">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <StackPanel Orientation="Horizontal"/>
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <CheckBox Content="{Binding Name}"
                        IsChecked="{Binding IsChecked}"
                        Margin="0 0 8 0"
                        VerticalAlignment="Center"
                        FontSize="12"/>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <ToggleButton Content="Assigned To Me"
                      IsChecked="{Binding DataContext.IsAssignedTo, RelativeSource={RelativeSource AncestorType=Window}}"
                      Style="{StaticResource BaseToggleButton}"
                      Margin="8 0 0 0"/>
      </WrapPanel>
    </Border>
    <!--#endregion-->

    <!--#region Gantt Table -->
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="220" MinWidth="160"/>
        <ColumnDefinition Width="4"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>

      <!-- LEFT: Task list per user -->
      <ListView Grid.Column="0"
                ItemsSource="{Binding TasksUserCollection}"
                BorderThickness="0"
                Background="Transparent"
                ScrollViewer.HorizontalScrollBarVisibility="Disabled">
        <ListView.GroupStyle>
          <GroupStyle>
            <GroupStyle.HeaderTemplate>
              <DataTemplate>
                <Border Background="{DynamicResource SecondaryBackgroundBrush}"
                        BorderThickness="0 0 0 1"
                        BorderBrush="{DynamicResource PrimaryBorderBrush}"
                        Padding="10 6">
                  <TextBlock Text="{Binding Name.Name}"
                             FontWeight="SemiBold" FontSize="12"/>
                </Border>
              </DataTemplate>
            </GroupStyle.HeaderTemplate>
          </GroupStyle>
        </ListView.GroupStyle>
        <ListView.ItemContainerStyle>
          <Style TargetType="ListViewItem">
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="BorderThickness" Value="0 0 0 1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBorderBrush}"/>
          </Style>
        </ListView.ItemContainerStyle>
        <ListView.ItemTemplate>
          <DataTemplate>
            <Grid Margin="10 4">
              <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
              </Grid.ColumnDefinitions>
              <!-- Status dot -->
              <Ellipse Width="8" Height="8" VerticalAlignment="Center">
                <Ellipse.Style>
                  <Style TargetType="Ellipse">
                    <Setter Property="Fill" Value="#2196F3"/>
                    <Style.Triggers>
                      <DataTrigger Binding="{Binding Status}" Value="0">
                        <Setter Property="Fill" Value="#4CAF50"/>
                      </DataTrigger>
                      <DataTrigger Binding="{Binding Status}" Value="1">
                        <Setter Property="Fill" Value="#FF9800"/>
                      </DataTrigger>
                      <DataTrigger Binding="{Binding Status}" Value="2">
                        <Setter Property="Fill" Value="#9C27B0"/>
                      </DataTrigger>
                    </Style.Triggers>
                  </Style>
                </Ellipse.Style>
              </Ellipse>
              <TextBlock Grid.Column="1" Text="{Binding OnlyName}"
                         FontSize="11" VerticalAlignment="Center"
                         TextTrimming="CharacterEllipsis"
                         ToolTip="{Binding Name}"/>
            </Grid>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>

      <GridSplitter Grid.Column="1" Width="4"
                    HorizontalAlignment="Stretch"
                    Background="{DynamicResource PrimaryBorderBrush}"/>

      <!-- RIGHT: Gantt bars over day columns -->
      <Grid Grid.Column="2">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto"/>
          <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Day headers -->
        <ScrollViewer HorizontalScrollBarVisibility="Hidden"
                      VerticalScrollBarVisibility="Disabled"
                      Name="DayHeaderScroll">
          <ItemsControl ItemsSource="{Binding Days}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal"/>
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <Border Width="50" Padding="2 6"
                        BorderThickness="0 0 1 1"
                        BorderBrush="{DynamicResource PrimaryBorderBrush}"
                        Background="{DynamicResource SecondaryBackgroundBrush}">
                  <StackPanel>
                    <TextBlock Text="{Binding Name, StringFormat=ddd}"
                               FontSize="9" Opacity="0.5"
                               HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Name, StringFormat=dd}"
                               FontSize="12" HorizontalAlignment="Center"/>
                  </StackPanel>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>

        <!-- Gantt bars: one row per task, width = Day property -->
        <ScrollViewer Grid.Row="1"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto">
          <ItemsControl ItemsSource="{Binding TasksUserCollection}">
            <ItemsControl.GroupStyle>
              <GroupStyle>
                <GroupStyle.HeaderTemplate>
                  <DataTemplate>
                    <Border Height="29"
                            Background="{DynamicResource SecondaryBackgroundBrush}"
                            BorderThickness="0 0 0 1"
                            BorderBrush="{DynamicResource PrimaryBorderBrush}"/>
                  </DataTemplate>
                </GroupStyle.HeaderTemplate>
              </GroupStyle>
            </ItemsControl.GroupStyle>
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <!-- Each row is 32px tall matching the left list -->
                <Canvas Height="32">
                  <Border Canvas.Left="{Binding Index}"
                          Width="{Binding Width}"
                          Height="20"
                          Canvas.Top="6"
                          CornerRadius="4"
                          ToolTip="{Binding Name}">
                    <Border.Style>
                      <Style TargetType="Border">
                        <Setter Property="Background" Value="#2196F3"/>
                        <Setter Property="Opacity" Value="0.8"/>
                        <Style.Triggers>
                          <DataTrigger Binding="{Binding Status}" Value="0">
                            <Setter Property="Background" Value="#4CAF50"/>
                          </DataTrigger>
                          <DataTrigger Binding="{Binding Status}" Value="1">
                            <Setter Property="Background" Value="#FF9800"/>
                          </DataTrigger>
                          <DataTrigger Binding="{Binding Status}" Value="2">
                            <Setter Property="Background" Value="#9C27B0"/>
                          </DataTrigger>
                        </Style.Triggers>
                      </Style>
                    </Border.Style>
                    <TextBlock Text="{Binding OnlyName}"
                               FontSize="10" Foreground="White"
                               VerticalAlignment="Center" Margin="4 0"
                               TextTrimming="CharacterEllipsis"/>
                  </Border>
                </Canvas>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </Grid>
    </Grid>
    <!--#endregion-->
  </Grid>
</UserControl>
