<Window
  x:Class="MultiTool.CreateRebarDetail.R_CreateRebarDetailView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:local="clr-namespace:MultiTool.CreateRebarDetail"
  xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
  Title="R_CreateRebarDetailView"
  Height="200"
  Width="300"
  Name="window">
  <i:Interaction.Triggers>
    <i:EventTrigger
      EventName="Loaded">
      <i:InvokeCommandAction
        Command="{Binding ElementName=window,Path=DataContext.LoadedCommand}" />
    </i:EventTrigger>
  </i:Interaction.Triggers>
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition
        Width="Auto" />
      <ColumnDefinition
        Width="*" />
    </Grid.ColumnDefinitions>
    <StackPanel>
      
    </StackPanel>
    <StackPanel
      Grid.Column="2"
      Orientation="Horizontal"
      HorizontalAlignment="Right">
      <Button
        Content="Select Link"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Width="80"
        Height="30"
        Margin="10"
        Command="{Binding ElementName=window, Path=DataContext.RunLinkCommand}" />
      <Button
        Content="Select"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Width="80"
        Height="30"
        Margin="10"
        Command="{Binding ElementName=window, Path=DataContext.RunCommand}" />
    </StackPanel>
    
  </Grid>
</Window>
