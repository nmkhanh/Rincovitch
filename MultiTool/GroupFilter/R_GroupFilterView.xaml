<Window
  x:Class="MultiTool.GroupFilter.R_GroupFilterView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:local="clr-namespace:MultiTool.GroupFilter"
  xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
  Title="R_GroupFilterView"
  Height="Auto"
  Width="450"
  Name="window"
  Icon="C:\ProgramData\Autodesk\ApplicationPlugins\Rincovitch.bundle\Contents\Resources\RincovitchIcon.png">
  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary
          Source="pack://application:,,,/MultiTool;component/Converter/Converter.xaml" />
        <ResourceDictionary
          Source="pack://application:,,,/MultiTool;component/Template/Button.xaml" />
        <ResourceDictionary
          Source="pack://application:,,,/MultiTool;component/Template/DataGrid.xaml" />
        <ResourceDictionary
          Source="pack://application:,,,/MultiTool;component/Template/CheckBox.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Window.Resources>
  <i:Interaction.Triggers>
    <i:EventTrigger
      EventName="Loaded">
      <i:InvokeCommandAction
        Command="{Binding ElementName=window,Path=DataContext.LoadedCommand}" />
    </i:EventTrigger>
  </i:Interaction.Triggers>
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition
        Height="*" />
      <RowDefinition
        Height="Auto" />
    </Grid.RowDefinitions>
    <Grid>
      <DataGrid
        Height="Auto"
        SelectedItem="{Binding VM.Type}"
        ItemsSource="{Binding VM.Type_Sort}">
        <DataGrid.RowStyle>
          <Style
            TargetType="DataGridRow"
            BasedOn="{StaticResource DataGridRowNew}">
            <Setter
              Property="Background"
              Value="{Binding BG}" />
          </Style>
        </DataGrid.RowStyle>
        <DataGrid.Columns>
          <DataGridTemplateColumn
            Header="Type"
            Width="*">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <TextBlock
                  Margin="5"
                  VerticalAlignment="Center"
                  Text="{Binding Name}" />
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
          <DataGridTemplateColumn
            Header="Count"
            Width="100">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <TextBlock
                  Margin="5"
                  VerticalAlignment="Center"
                  Text="{Binding Count}" />
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
      </DataGrid>
    </Grid>
    <StackPanel
      Grid.Row="1"
      Orientation="Horizontal"
      HorizontalAlignment="Right">
      <Button
        Content="Color"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Width="80"
        Height="30"
        Margin="10"
        Command="{Binding ElementName=window, Path=DataContext.ColorCommand}" />
      <Button
        Content="Refresh"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Width="80"
        Height="30"
        Margin="10"
        Command="{Binding ElementName=window, Path=DataContext.RefreshCommand}" />
      <Button
        Content="Select"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Width="80"
        Height="30"
        Margin="10"
        Command="{Binding ElementName=window, Path=DataContext.SelectCommand}" />
      <Button
        Content="Highlight"
        HorizontalAlignment="Right"
        VerticalAlignment="Bottom"
        Width="80"
        Height="30"
        Margin="10"
        Command="{Binding ElementName=window, Path=DataContext.HighlightCommand}" />
    </StackPanel>
    
  </Grid>
</Window>
